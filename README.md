# Smart Iconfont - VS Code 扩展

一个智能的 VS Code 扩展，用于自动补全 iconfont 图标并可显示图标预览。

## 功能特性

-  **自动扫描**：自动扫描工作区中的 `iconfont.ttf` 和 `iconfont.json` 文件
-  **图标预览**：在侧边栏面板中显示所有可用图标的可视化预览
-  **自动触发**：输入 "icon" 关键词时自动显示图标面板
-  **快速插入**：点击图标即可快速插入到代码中

## 使用
安装后，输入 `icon-`即可调出菜单 

## TODO
- 目前只支持iconfont.cn